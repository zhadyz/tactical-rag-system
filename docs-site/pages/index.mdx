---
title: Apollo RAG - GPU-Accelerated Document Intelligence
description: Production-grade RAG platform with CUDA optimization, adaptive retrieval, and enterprise deployment
---

import { Card, FeatureCard } from '@/components/Card'
import { Callout } from '@/components/Callout'
import { MetricCard } from '@/components/MetricCard'
import { HeroSection } from '@/components/HeroSection'
import { PerformanceMetrics } from '@/components/PerformanceMetrics'
import { KeyFeatures } from '@/components/KeyFeatures'
import { ArchitectureOverview } from '@/components/ArchitectureOverview'
import { WhyApollo } from '@/components/WhyApollo'
import { BenchmarksTable } from '@/components/BenchmarksTable'
import { CTASection } from '@/components/CTASection'
import { Zap, Cpu, Database, Lock, Gauge, Box, ArrowRight, Github, Sparkles } from 'lucide-react'

<HeroSection />

<PerformanceMetrics />

<Callout type="success" title="Production Ready">
  Apollo v4.1 includes battle-tested optimizations from real-world deployments processing millions
  of documents. GPU acceleration delivers **10x faster** retrieval compared to CPU-only systems.
</Callout>

<KeyFeatures />

<div className="mx-auto max-w-4xl">

## Quick Start

Get Apollo running in under 5 minutes:

```bash
# Clone the repository
git clone https://github.com/yourusername/apollo-rag.git
cd apollo-rag

# Launch with Docker Compose (includes GPU support)
docker compose up -d

# Upload your first document
curl -X POST http://localhost:8000/upload \
  -F "file=@document.pdf" \
  -H "Authorization: Bearer your-api-key"

# Ask a question
curl -X POST http://localhost:8000/query \
  -H "Content-Type: application/json" \
  -d '{"query": "What is the main topic?"}'
```

See the [Getting Started](/getting-started) guide for detailed instructions.

</div>

<ArchitectureOverview />

<WhyApollo />

<BenchmarksTable />

<div className="mx-auto max-w-4xl">

## Community & Support

- **Documentation**: You're reading it! Comprehensive guides for every use case
- **GitHub**: [Report issues](https://github.com/yourusername/apollo-rag/issues) and contribute
- **Discord**: Join our [community](https://discord.gg/apollo-rag) for real-time help
- **Enterprise**: Contact us for SLA, custom features, and dedicated support

## License

Apollo RAG is open source under the [MIT License](https://github.com/yourusername/apollo-rag/blob/main/LICENSE).

</div>

---

<CTASection />
